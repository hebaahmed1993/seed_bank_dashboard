
enum DashboardTab {
  home,         // الرئيسية
  storefront,   // واجهة التطبيق (جديد)
  plants,       // النباتات والبذور
  categories,   // التصنيفات
  inventory,    // المخزون (جديد)
  suppliers,    // الموردين (جديد)
  users,        // المستخدمين
  orders,       // الطلبات
  reports,      // التقارير
  settings      // الإعدادات
}